interface:
  display_name: "Pull"
  short_description: "Repo-specific patterns and workflows for pull"
  default_prompt: "Use the pull repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true